---
title: "A title describing the folder-based data story"
description: "A one-or-two sentence summary of what the analysis shows."
date: 2026-06-18
author: "Author Name"
---

```{r}
#| label: setup
# Shared tokens (generated from sass/_tokens.scss) + zola_style() plotly theme.
source(file.path(Sys.getenv("QUARTO_PROJECT_DIR"), "_setup.R"))
library(readr)

# This story is self-contained: its dataset lives in ./data/ next to this .qmd.
# `execute-dir: file` (in _quarto.yml) makes the working directory this folder,
# so the relative path resolves regardless of where the render is invoked from.
series <- readr::read_csv("data/series.csv", show_col_types = FALSE)
```

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor
incididunt ut labore et dolore magna aliqua — an opening paragraph introducing
the story's question and why it matters.

## A section heading describing the data

Ut enim ad minim veniam, quis nostrud exercitation. The chart below reads from
the story's own `data/series.csv` and uses **synthetic placeholder data**, not
real regional figures.

```{r}
#| label: fig-series
#| fig-cap: "A caption describing what this chart would show"
plot_ly(series, x = ~year, y = ~value, type = "scatter", mode = "lines",
        line = list(color = black, width = 2)) |>
  zola_style(ytitle = "A value label", xtitle = "Year")
```

A paragraph interpreting the (placeholder) visualization, in lorem ipsum dolor
sit amet, consectetur adipiscing elit.

::: {.callout-note}
A short callout describing a caveat or a data-source note.
:::
